$(document).ready(function() {                                                              
    $("button#search").button();
    $("button#search").click(function() {
        alert($("input#searchText")[0].value);
    });
});
